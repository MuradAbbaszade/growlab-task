CREATE TABLE `attribute` (
                             `id` BIGINT AUTO_INCREMENT PRIMARY KEY,
                             `attribute` varchar(256),
                             `module_id` BIGINT
);