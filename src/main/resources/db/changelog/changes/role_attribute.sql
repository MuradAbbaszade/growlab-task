CREATE TABLE `role_attribute` (
                                  `id` integer PRIMARY KEY,
                                  `role_id` integer,
                                  `attribute_id` integer
);